import { requireAdmin } from './_guard.js';import { getJSON } from '../_store.js';export default async function handler(req,res){if(!requireAdmin(req,res))return;const topics=await getJSON('topics')||[];const featured=await getJSON('featured');res.status(200).json({topics,featured:featured||null});}